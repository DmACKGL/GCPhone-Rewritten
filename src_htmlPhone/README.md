1. Install Yarn packages
   ```sh
   yarn install
   ```
2. Build the project
   1. For production
      ```sh
      yarn run build
      ```
   2. For development
      ```
      yarn run dev
      ```
